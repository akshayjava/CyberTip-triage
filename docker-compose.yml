version: "3.9"

services:
  # ── Application ──────────────────────────────────────────────────────────────
  app:
    build: .
    container_name: cybertip-app
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      DB_MODE: postgres
      DATABASE_URL: postgresql://cybertip:${DB_PASSWORD:-changeme}@postgres:5432/cybertip
      QUEUE_MODE: bullmq
      REDIS_HOST: redis
      REDIS_PORT: 6379
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - tip_downloads:/tmp/cybertip-ids
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Database ──────────────────────────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    container_name: cybertip-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cybertip
      POSTGRES_USER: cybertip
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/db/migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cybertip"]
      interval: 10s
      timeout: 5s
      retries: 5
    # Never expose DB port externally
    expose:
      - "5432"

  # ── Queue / Cache ─────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: cybertip-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-changeme} --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--pass", "${REDIS_PASSWORD:-changeme}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    expose:
      - "6379"

  # ── Reverse Proxy (HTTPS termination) ────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: cybertip-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./setup/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./setup/certs:/etc/nginx/certs:ro
      - nginx_logs:/var/log/nginx
    depends_on:
      - app
    profiles:
      - production  # Only in production: docker compose --profile production up

  # ── Ollama (local LLM server for offline/air-gap mode) ───────────────────────
  # Activated by: docker compose --profile offline up
  # Before going air-gap, pull Gemma models on a connected machine:
  #   ollama pull gemma3:27b && ollama pull gemma3:12b && ollama pull gemma3:4b
  # Then save the Ollama data volume and restore it on the air-gap machine.
  ollama:
    image: ollama/ollama:latest
    container_name: cybertip-ollama
    restart: unless-stopped
    volumes:
      - ollama_models:/root/.ollama
    expose:
      - "11434"
    # GPU support (uncomment if NVIDIA GPU available):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    profiles:
      - offline   # Only started in offline mode: docker compose --profile offline up

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  tip_downloads:
    driver: local
  nginx_logs:
    driver: local
  ollama_models:
    driver: local   # Persists Gemma model weights across container restarts
